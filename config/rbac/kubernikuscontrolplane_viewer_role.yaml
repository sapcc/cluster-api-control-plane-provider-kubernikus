# permissions for end users to view kubernikuscontrolplanes.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: kubernikuscontrolplane-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: cluster-api-control-plane-provider-kubernikus
    app.kubernetes.io/part-of: cluster-api-control-plane-provider-kubernikus
    app.kubernetes.io/managed-by: kustomize
  name: kubernikuscontrolplane-viewer-role
rules:
- apiGroups:
  - controlplane.cluster.x-k8s.io
  resources:
  - kubernikuscontrolplanes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - controlplane.cluster.x-k8s.io
  resources:
  - kubernikuscontrolplanes/status
  verbs:
  - get
